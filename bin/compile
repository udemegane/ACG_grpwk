#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found ./run-backserver.sh"

if [ ! -s $1/run-backserver.sh ]; then
  echo "run-backserver.sh was empty" | indent
  exit 1
fi

ACG_PRODUCTION_STAGE='production' bash run-backserver.sh --init -rnb
